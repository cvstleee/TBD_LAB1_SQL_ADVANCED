// src/plugins/vuetify.js

import { createVuetify } from 'vuetify'; // Importar desde 'vuetify'
import 'vuetify/styles'; // Importar los estilos de Vuetify

const vuetify = createVuetify(); // Crear una instancia de Vuetify

export default vuetify; // Exportar la instancia